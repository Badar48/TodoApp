<% if @todo.errors.any? %>
<h2>Following errors prevent todo to save</h2>
	<ul>
		<% @todo.errors.full_messages.each do|msg| %>
			<li> <%= msg %> </li>
		 <% end %>
	</ul>
<% end %>

<%= form_for @todo do |f| %>
<p>
<%= f.label :name %>
<%= f.text_field :name %>
</p>
<P>
	<%= f.label :description%>
	<%= f.text_field :description %>
</P>
<p>
	<%= f.submit %>
</p>
	<% end %>
	<%= link_to "Back to listing", todos_path(@todo) %>